#include <iostream>
#include "Predict.h"
#include "MerkelMain.h"
#include "TradeBot.h"
using namespace std;

int main()
{
//   MerkelMain app;
  // app.init();


    //Predict p;
    //string s = p.getOrderBook().getEarliestTime();
    
    
    //s = p.getOrderBook().getNextTime(s);
    //s = p.getOrderBook().getNextTime(s);
    //OrderBookEntry obe =  p.PredictTrend("ETH/BTC",s);
   
    

   TradeBot tb; 
   tb.Start();

}
